clear
clc

syms 'x'
f = @(x) log(x)+cos(x)-1;
df =@(x) sin(x) - 1/x;
x = 1;
%dif =@(x) diff(f2);
y= x-f(x)/df(x);
root = abs(f(y));
epsilon = 1e-7; 

while root > epsilon
    x1=x-(f(x)/df(x));
    x = x1;
    root = abs(x-x1);
    s = x1;
end

fprintf('The root is:  %.2f ',s)